namespace Tuto
{
    public class Event_Wait
    {
        private float _durationToWait;

        public Event_Wait(float durationToWait)
        {
            _durationToWait = durationToWait;
        }
    }
}